# !/usr/bin/env bash

# refer to https://elixirforum.com/t/edeliver-custom-commands/1511/4

REQUIRED_CONFIGS+=("APP")

run() {
  authorize_hosts
  # remote executes commands on the deploy hosts, asynchronously
  __remote "
    ./bin/dustbin migrate
  "
}
